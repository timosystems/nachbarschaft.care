<form method="POST" action="/src/sendpwreset.php">
    <div class="modal" tabindex="-1" role="dialog" id="modal_pwreset_request">
        <div class="modal-dialog modal-lg" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title"><i class="fas fa-key fa-fw mr-1"></i>Passwort vergessen</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    <p>Bitte gebe deine E-Mailadresse ein. Du erhältst anschließend einen Link, mit dem du dein Passwort zurücksetzen kannst.</p>
                        <div class="input-group mb-2">
                            <div class="input-group-prepend">
                                <span class="input-group-text" style="width: 120px;">E-Mail:</span>
                            </div>
                            <input required="required" id="pwre_email" name="pwre_email" class="form-control" type="email" aria-label="E-Mail">
                        </div>
                        <div class="card mb-2">
                            <div class="card-body text-center p-2">
                                {{ captcha|raw }}
                            </div>
                        </div>
                        <div class="alert alert-secondary mb-0 p-2"><span><i class="fas fa-info-circle mr-1"></i>Bitte gebe den Inhalt des Bildes in das Textfeld ein, damit wir wissen, dass du ein Mensch bist!</span></div>        
                </div>
                <div class="btn-group">
                    <button class="btn btn-block btn-success no-radius" type="Submit"><i class="fas fa-check fa-fw mr-1"></i>Neues Passwort anfordern</button>
                </div>
                <div class="modal-footer">
                    {{ include('modals/parts/modal.part.footer.twig') }}
                </div>
            </div>
        </div>
    </div>
</form>