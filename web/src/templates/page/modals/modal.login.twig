<div class="modal" tabindex="-1" role="dialog" id="modal_login">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"><i class="fas fa-sign-in-alt mr-1"></i>Login</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form method="POST" id="login" action="/src/login.php">
                    <div class="input-group mb-2">
                        <div class="input-group-prepend">
                            <span class="input-group-text" style="width: 120px;">E-Mail:</span>
                        </div>
                        <input required="required" id="log_email" name="log_email" class="form-control" type="email" aria-label="Username">
                    </div>
                    <div class="input-group mb-2">
                        <div class="input-group-prepend">
                            <span class="input-group-text" style="width: 120px;">Passwort:</span>
                        </div>
                        <input required="required" id="log_password" name="log_password" class="form-control" type="password" aria-label="Passwort">
                    </div>
                </form>
                <span style="text-align: center; display: block; width: 100%;" class="mt-3"><a href="#" onClick="showDiag('pwreset_request')">Passwort vergessen?</a></span>
            </div>
            <div class="btn-group">
                <button form="login" class="btn btn-success no-radius" type="Submit"><i class="fas fa-check mr-1"></i>Anmelden</button>
                <button class="btn btn-primary no-radius" type="button" onClick="showDiag('givehelp')"><i class="fas fa-plus mr-1"></i>Registrieren</button>
            </div>
            <div class="modal-footer">
                {{ include('modals/parts/modal.part.footer.twig') }}
            </div>
        </div>
    </div>
</div>