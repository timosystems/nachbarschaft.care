<div class="modal" tabindex="-1" role="dialog" id="modal_dopasswordreset">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Passwort vergessen</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Bitte gebe dein neues Passwort ein</p>
                <form method="POST" action="/src/pwreset.php">
                    <?php if(isset($_GET['token'])){ ?>
                    <input id="dopwre_token" name="dopwre_token" class="form-control" type="hidden" value="<?php echo $_GET['token']; ?>">
                    <?php } ?>      
                    <div class="input-group mb-2">
                        <div class="input-group-prepend">
                            <span class="input-group-text" style="width: 120px;">Passwort:</span>
                        </div>
                        <input required="required" id="dopwre_newpassword" name="dopwre_newpassword" class="form-control" type="password" aria-label="Neues Passwort">
                    </div>
                    <p class="mb-2"><small>Mindestens 8 Zeichen mit Buchstaben und Zahlen</small></p>
                    <button class="btn btn-block btn-success" type="Submit">Passwort neu setzen</button>
                </form>
            </div>
            <div class="modal-footer">
                {{ include('modals/parts/modal.part.footer.twig') }}
            </div>
        </div>
    </div>
</div>
{% if modal == 'dopwreset' %}
<script type="text/javascript">
    $('#modal_dopasswordreset').modal();
</script>
{% endif %}