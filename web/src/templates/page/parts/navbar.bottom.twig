<nav class="navbar fixed-bottom navbar-expand-lg navbar-light bg-light p-2">
    <form class="form-inline" method="GET" action="/" style="width: 100%;">
        <div class="input-group" style="width: 100%;">
            <div class="input-group-prepend">
                <span class="input-group-text">Hilfe in</span>
            </div>
            <input required="required" id="plz_footer" name="plz" class="form-control" type="search" placeholder="PLZ" value="{{ data.selection.plz }}" aria-label="Nach Postleitzahl suchen">
            <div class="input-group-append">
                <button class="btn btn-primary" type="submit">Suchen</button>
            </div>
            <div class="input-group-append">
                {% if user.login == false %}
                <button class="btn btn-success" onclick="diagGiveHelp('footer');" type="button">Bieten</button>
                {% else %}
                <button class="btn btn-success" onclick="diagAddHelp('footer');" type="button">Bieten</button
                {% endif %}
            </div>
        </div>
    </form>
</nav>